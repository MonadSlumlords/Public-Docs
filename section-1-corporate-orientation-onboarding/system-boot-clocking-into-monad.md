# 🔌 System Boot: Clocking into Monad

**"You can't execute a mass eviction if you can't connect to the server. Get online, Junior."**

Welcome to the command line. Slumlords OS is not a web browser game; it is a direct, unfiltered terminal connection to the BlackCoq extraction protocols. To begin managing your empire, you must first initialize your node and sync with the blockchain.

***

## 🔌 Node Initialization (The Monad Migration)

Slumlords OS requires an active Web3 provider (e.g., MetaMask, Rabby, Core) to sync your local terminal with the on-chain ledger.

We operate strictly on the **Monad** network. Why Monad? Because traditional blockchains are too slow. When a tenant is late on their `$RENT`, we need a parallelized EVM that can process the eviction notice, seize their security deposit, and market-sell their belongings in under a second. Sub-second finality isn't a luxury; it's a corporate requirement.

***

## 🌐 Network Parameters (The Local Grid)

If your wallet is stuck on some legacy network, the Slumlords OS features an automated network enforcer. Upon connection, the OS will aggressively push a request to your provider to switch to Monad. We do not accept testnet monopoly money here.

For operators who insist on manual configuration, input the following parameters into your Web3 wallet:

* **Network Name:** Monad Mainnet
* **Chain ID:** 143
* **Native Currency:** MON
* **RPC URL:** https://rpc.monad.xyz
* **Block Explorer:** https://monadvision.com

***

## 🔑 Authentication Sequence (Clocking In)

To initialize your session and access the proprietary asset management tools, complete the following boot sequence:

{% stepper %}
{% step %}
### Access Terminal

Navigate to the Slumlords OS interface.
{% endstep %}

{% step %}
### Ping the Server

Locate the flashing **CONNECT\_NODE** status indicator in the bottom-right taskbar.
{% endstep %}

{% step %}
### Sign the Waiver

Click the module and approve the connection request in your Web3 wallet. You are giving us read access. Don't worry, we only steal your money when you explicitly sign a transaction.
{% endstep %}

{% step %}
### Enforce Network

If prompted by the OS network enforcer, blindly approve the network switch to Monad.
{% endstep %}
{% endstepper %}

Upon successful authentication, the terminal will establish a secure uplink, sync your corporate balances (`MON`, `WMON`, `$RENT`), verify your current contract allowances, and grant you root access to the left-hand Disk Drive modules.

***

## 🏗️ Core Contract Architecture (The Engine Room)

For advanced operators and paranoid auditors conducting ledger verification, the OS routes all logic through five proprietary smart contracts. Memorize them.

* **`GAME_NFT` (The Zoning Board):** Handles the ERC-721 base property generation, tier lottery logic, district zoning, and the predatory minting pricing curves.
* **`UPGRADE_NFT` (The Renovation Racket):** Manages the ERC-721 renovation modules. This contract strictly enforces the manufactured scarcity of our 20-tier upgrade tech tree.
* **`PORTFOLIO` (The Collection Agency):** The central vault. This is the heart of the operation. It manages your property lockups, calculates your structural fatigue, deducts your protocol taxes, and spits out your daily `$RENT` yield.
* **`GAME_TOKEN` (The Federal Reserve):** The `$RENT` ERC-20 utility and reward token contract. It tracks the hyper-deflationary supply and executes the burn protocols.
* **`ROUTER` (The Automated Launderer):** The integrated decentralized exchange router. This handles the automated market-sweeps, turning your `MON` into `WMON` and market-buying `$RENT` to pump the bags.
